package com.example.provaapiyoutube;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

/**
 * Aquesta classe de noment només hi té una part gràfica, fins que
 * fem la base de dades.
 */
public class AddVideos extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_videos);
    }
}
